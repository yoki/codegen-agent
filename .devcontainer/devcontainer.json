{
    "name": "Python 3.13",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "mounts": [
        // To avoid re-installation of VS code extensions for every reload
        "type=volume,source=vscode-${localWorkspaceFolderBasename}-server,target=/root/.vscode-server",
        // GEMINI_API key
        "type=bind,source=/mnt/c/mydata/workspace/configs/secrets/codegen_agent,target=/secrets/codegen_agent,readonly"
    ],
    "postCreateCommand": "uv pip install --system -e .[jupyter]",
    // "postStartCommand": "bash -l -c start-docker",
    "privileged": true,
    "containerEnv": {
        "CODEGEN_AGENT_STATE": "/workspaces/${localWorkspaceFolderBasename}/codegen_agent_state"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter",
                "charliermarsh.ruff"
            ],
            "settings": {
                "files.eol": "\n",
                "remote.containers.forwardGpg": false, // saves time
                "remote.containers.forwardX11": false, // saves time
                // Python built-ins disabled
                "python.linting.enabled": false,
                "python.formatting.provider": "none",
                "python.linting.pylintEnabled": false,
                "python.linting.flake8Enabled": false,
                "python.linting.mypyEnabled": false,
                "[python]": {
                    "editor.defaultFormatter": "charliermarsh.ruff",
                    "editor.formatOnSave": true,
                    "editor.codeActionsOnSave": {
                        "source.fixAll": "always",
                        "source.organizeImports": "never"
                    }
                }
            }
        }
    }
}